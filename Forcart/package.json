{
  "dependencies": {
    "json-server": "^0.16.3"
  },
  "scripts": {
    "start": "json-server --watch db.json --port 3006",
    "json-server": "./node_modules/.bin/json-server --watch db.json --port 3006"
  }
}
