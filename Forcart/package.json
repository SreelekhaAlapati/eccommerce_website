
{
  "name": "my-app",
  "version": "1.0.0",
  "description": "My app using json-server",
  "main": "index.js",
  "dependencies": {
    "json-server": "^0.16.3"
  },
  "scripts": {
    "start": "json-server --watch db.json --port 3006"
  }
}